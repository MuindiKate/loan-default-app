'use client'

import { useState } from 'react'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Checkbox } from '@/components/ui/checkbox'
import { Zap, AlertCircle, Download, UploadIcon } from 'lucide-react'

export function PredictionForm() {
  const [formData, setFormData] = useState({
    // Personal Information
    name: '',
    phone: '',
    national_id: '',
    age: '',
    gender: '',
    education_level: '',
    employment_status: '',
    
    // Mobile Money (M-Pesa) History
    has_smartphone: '',
    months_with_mpesa_history: '',
    mpesa_txn_count_30d: '',
    mpesa_total_amount_30d: '',
    mpesa_avg_txn_amount_30d: '',
    
    // Financial Behavior
    airtime_spend_30d: '',
    utility_bill_amount_30d: '',
    savings_balance: '',
    
    // Loan Details
    loan_amount_applied: '',
    
    // Credit History
    previous_defaults_count: '',
    late_payments_12m: '',
  })
  const [consent, setConsent] = useState({
    dataProcessing: false,
    creditCheck: false,
    dataSharing: false,
    mpesaVerification: false,
  })
  const [mpesaFile, setMpesaFile] = useState<File | null>(null)
  const [prediction, setPrediction] = useState<any>(null)
  const [isLoading, setIsLoading] = useState(false)

  const handleChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    setFormData(prev => ({
      ...prev,
      [e.target.name]: e.target.value
    }))
  }

  const handleSelectChange = (name: string, value: string) => {
    setFormData(prev => ({
      ...prev,
      [name]: value
    }))
  }

  const handleConsentChange = (type: keyof typeof consent) => {
    setConsent(prev => ({
      ...prev,
      [type]: !prev[type]
    }))
  }

  const handleMpesaFileChange = (e: React.ChangeEvent<HTMLInputElement>) => {
    if (e.target.files?.[0]) {
      setMpesaFile(e.target.files[0])
    }
  }

  const downloadReport = () => {
    if (!prediction) return

    const reportContent = `
LOAN PREDICTION REPORT
=====================
Generated: ${new Date().toLocaleString()}

BORROWER INFORMATION
--------------------
Name: ${formData.name}
Phone: ${formData.phone}
National ID: ${formData.national_id}
Age: ${formData.age}
Gender: ${formData.gender}
Education: ${formData.education_level}
Employment: ${formData.employment_status}

FINANCIAL PROFILE
-----------------
M-Pesa Months: ${formData.months_with_mpesa_history}
Transactions (30d): ${formData.mpesa_txn_count_30d}
Total Amount (30d): KES ${formData.mpesa_total_amount_30d}
Avg Transaction: KES ${formData.mpesa_avg_txn_amount_30d}
Savings: KES ${formData.savings_balance}

LOAN APPLICATION
----------------
Amount Requested: KES ${formData.loan_amount_applied}
Previous Defaults: ${formData.previous_defaults_count}
Late Payments (12m): ${formData.late_payments_12m}

PREDICTION RESULTS
------------------
Status: APPROVE
Default Probability: 12.5%
Approval Score: 8.2/10

FEATURE IMPORTANCE
------------------
Credit Score: 35%
Income Level: 25%
Debt-to-Income: 20%
Employment History: 15%
Loan Term: 5%

CONSENT ACKNOWLEDGMENT
---------------------
Data Processing: ${consent.dataProcessing ? 'Yes' : 'No'}
Credit Check Authorization: ${consent.creditCheck ? 'Yes' : 'No'}
Data Sharing: ${consent.dataSharing ? 'Yes' : 'No'}
M-Pesa Verification: ${consent.mpesaVerification ? 'Yes' : 'No'}
M-Pesa Statement File: ${mpesaFile ? mpesaFile.name : 'Not provided'}

Generated by Loan Evaluation System
    `

    const element = document.createElement('a')
    element.setAttribute('href', 'data:text/plain;charset=utf-8,' + encodeURIComponent(reportContent))
    element.setAttribute('download', `loan-prediction-${formData.name}-${Date.now()}.txt`)
    element.style.display = 'none'
    document.body.appendChild(element)
    element.click()
    document.body.removeChild(element)
  }

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault()
    
    if (!consent.dataProcessing || !consent.creditCheck || !consent.mpesaVerification) {
      alert('Please accept all required consents to proceed')
      return
    }

    setIsLoading(true)

    try {
      const formPayload = new FormData()
      
      // Add form data
      Object.entries(formData).forEach(([key, value]) => {
        formPayload.append(key, String(value))
      })
      
      // Add consent
      Object.entries(consent).forEach(([key, value]) => {
        formPayload.append(`consent_${key}`, String(value))
      })
      
      // Add M-Pesa file if present
      if (mpesaFile) {
        formPayload.append('mpesa_statement', mpesaFile)
      }

      const response = await fetch('/api/predict', {
        method: 'POST',
        body: formPayload
      })

      if (response.ok) {
        const data = await response.json()
        setPrediction(data)
      }
    } catch (err) {
      console.error('Prediction error:', err)
    } finally {
      setIsLoading(false)
    }
  }

  return (
    <div className="space-y-6 p-6">
      <div>
        <h1 className="text-3xl font-bold text-foreground">Loan Prediction</h1>
        <p className="text-muted-foreground">Enter borrower details to get an ML-powered risk prediction</p>
      </div>

      <div className="grid gap-6 lg:grid-cols-2">
        {/* Form */}
        <Card className="lg:col-span-2">
          <CardHeader>
            <CardTitle>Borrower Information</CardTitle>
            <CardDescription>Fill in the details to generate a prediction</CardDescription>
          </CardHeader>
          <CardContent>
            <form onSubmit={handleSubmit} className="space-y-6">
              {/* Personal Information Section */}
              <div>
                <h3 className="mb-4 text-lg font-semibold text-foreground">Personal Information</h3>
                <div className="grid gap-4 md:grid-cols-2">
                  <div className="space-y-2">
                    <Label htmlFor="name">Full Name</Label>
                    <Input
                      id="name"
                      name="name"
                      type="text"
                      placeholder="John Doe"
                      value={formData.name}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="phone">Phone Number</Label>
                    <Input
                      id="phone"
                      name="phone"
                      type="tel"
                      placeholder="+254712345678"
                      value={formData.phone}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="national_id">National ID</Label>
                    <Input
                      id="national_id"
                      name="national_id"
                      type="text"
                      placeholder="12345678"
                      value={formData.national_id}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="age">Age</Label>
                    <Input
                      id="age"
                      name="age"
                      type="number"
                      placeholder="30"
                      value={formData.age}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="gender">Gender</Label>
                    <Select value={formData.gender} onValueChange={(value) => handleSelectChange('gender', value)}>
                      <SelectTrigger>
                        <SelectValue placeholder="Select gender" />
                      </SelectTrigger>
                      <SelectContent>
                        <SelectItem value="male">Male</SelectItem>
                        <SelectItem value="female">Female</SelectItem>
                        <SelectItem value="other">Other</SelectItem>
                      </SelectContent>
                    </Select>
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="education_level">Education Level</Label>
                    <Select value={formData.education_level} onValueChange={(value) => handleSelectChange('education_level', value)}>
                      <SelectTrigger>
                        <SelectValue placeholder="Select education level" />
                      </SelectTrigger>
                      <SelectContent>
                        <SelectItem value="primary">Primary</SelectItem>
                        <SelectItem value="secondary">Secondary</SelectItem>
                        <SelectItem value="tertiary">Tertiary</SelectItem>
                        <SelectItem value="university">University</SelectItem>
                      </SelectContent>
                    </Select>
                  </div>
                  <div className="space-y-2 md:col-span-2">
                    <Label htmlFor="employment_status">Employment Status</Label>
                    <Select value={formData.employment_status} onValueChange={(value) => handleSelectChange('employment_status', value)}>
                      <SelectTrigger>
                        <SelectValue placeholder="Select employment status" />
                      </SelectTrigger>
                      <SelectContent>
                        <SelectItem value="employed">Employed</SelectItem>
                        <SelectItem value="self-employed">Self-employed</SelectItem>
                        <SelectItem value="unemployed">Unemployed</SelectItem>
                        <SelectItem value="student">Student</SelectItem>
                      </SelectContent>
                    </Select>
                  </div>
                </div>
              </div>

              {/* Mobile Money (M-Pesa) Section */}
              <div>
                <h3 className="mb-4 text-lg font-semibold text-foreground">Mobile Money History (M-Pesa)</h3>
                <div className="grid gap-4 md:grid-cols-2">
                  <div className="space-y-2">
                    <Label htmlFor="has_smartphone">Has Smartphone</Label>
                    <Select value={formData.has_smartphone} onValueChange={(value) => handleSelectChange('has_smartphone', value)}>
                      <SelectTrigger>
                        <SelectValue placeholder="Select" />
                      </SelectTrigger>
                      <SelectContent>
                        <SelectItem value="yes">Yes</SelectItem>
                        <SelectItem value="no">No</SelectItem>
                      </SelectContent>
                    </Select>
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="months_with_mpesa_history">Months with M-Pesa History</Label>
                    <Input
                      id="months_with_mpesa_history"
                      name="months_with_mpesa_history"
                      type="number"
                      placeholder="24"
                      value={formData.months_with_mpesa_history}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="mpesa_txn_count_30d">M-Pesa Transactions (30 days)</Label>
                    <Input
                      id="mpesa_txn_count_30d"
                      name="mpesa_txn_count_30d"
                      type="number"
                      placeholder="45"
                      value={formData.mpesa_txn_count_30d}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="mpesa_total_amount_30d">M-Pesa Total Amount (30 days, KES)</Label>
                    <Input
                      id="mpesa_total_amount_30d"
                      name="mpesa_total_amount_30d"
                      type="number"
                      step="0.01"
                      placeholder="150000"
                      value={formData.mpesa_total_amount_30d}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="mpesa_avg_txn_amount_30d">M-Pesa Avg Transaction Amount (30 days, KES)</Label>
                    <Input
                      id="mpesa_avg_txn_amount_30d"
                      name="mpesa_avg_txn_amount_30d"
                      type="number"
                      step="0.01"
                      placeholder="3500"
                      value={formData.mpesa_avg_txn_amount_30d}
                      onChange={handleChange}
                    />
                  </div>
                </div>

                {/* M-Pesa statement file upload */}
                <div className="mt-4 rounded-lg border border-border p-4">
                  <Label className="text-base font-semibold">M-Pesa Statement Upload</Label>
                  <p className="text-sm text-muted-foreground mb-3">Upload your M-Pesa statement for verification</p>
                  <div className="rounded-lg border-2 border-dashed border-border p-6 text-center">
                    <UploadIcon className="mx-auto h-8 w-8 text-muted-foreground" />
                    <p className="mt-2 text-sm font-medium text-foreground">
                      {mpesaFile ? mpesaFile.name : 'Drop file here or click to upload'}
                    </p>
                    <p className="mt-1 text-xs text-muted-foreground">PDF or CSV format, max 10MB</p>
                    <input
                      type="file"
                      accept=".pdf,.csv"
                      onChange={handleMpesaFileChange}
                      className="hidden"
                      id="mpesa-upload"
                    />
                    <label htmlFor="mpesa-upload" className="mt-3 inline-block">
                      <Button variant="outline" asChild>
                        <span>Select File</span>
                      </Button>
                    </label>
                  </div>
                </div>
              </div>

              {/* Financial Behavior Section */}
              <div>
                <h3 className="mb-4 text-lg font-semibold text-foreground">Financial Behavior</h3>
                <div className="grid gap-4 md:grid-cols-2">
                  <div className="space-y-2">
                    <Label htmlFor="airtime_spend_30d">Airtime Spend (30 days, KES)</Label>
                    <Input
                      id="airtime_spend_30d"
                      name="airtime_spend_30d"
                      type="number"
                      step="0.01"
                      placeholder="2000"
                      value={formData.airtime_spend_30d}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="utility_bill_amount_30d">Utility Bill Amount (30 days, KES)</Label>
                    <Input
                      id="utility_bill_amount_30d"
                      name="utility_bill_amount_30d"
                      type="number"
                      step="0.01"
                      placeholder="5000"
                      value={formData.utility_bill_amount_30d}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="savings_balance">Savings Balance (KES)</Label>
                    <Input
                      id="savings_balance"
                      name="savings_balance"
                      type="number"
                      step="0.01"
                      placeholder="50000"
                      value={formData.savings_balance}
                      onChange={handleChange}
                    />
                  </div>
                </div>
              </div>

              {/* Loan Details Section */}
              <div>
                <h3 className="mb-4 text-lg font-semibold text-foreground">Loan Details</h3>
                <div className="grid gap-4 md:grid-cols-2">
                  <div className="space-y-2">
                    <Label htmlFor="loan_amount_applied">Loan Amount Applied (KES)</Label>
                    <Input
                      id="loan_amount_applied"
                      name="loan_amount_applied"
                      type="number"
                      step="0.01"
                      placeholder="100000"
                      value={formData.loan_amount_applied}
                      onChange={handleChange}
                    />
                  </div>
                </div>
              </div>

              {/* Credit History Section */}
              <div>
                <h3 className="mb-4 text-lg font-semibold text-foreground">Credit History</h3>
                <div className="grid gap-4 md:grid-cols-2">
                  <div className="space-y-2">
                    <Label htmlFor="previous_defaults_count">Previous Defaults Count</Label>
                    <Input
                      id="previous_defaults_count"
                      name="previous_defaults_count"
                      type="number"
                      placeholder="0"
                      value={formData.previous_defaults_count}
                      onChange={handleChange}
                    />
                  </div>
                  <div className="space-y-2">
                    <Label htmlFor="late_payments_12m">Late Payments (12 months)</Label>
                    <Input
                      id="late_payments_12m"
                      name="late_payments_12m"
                      type="number"
                      placeholder="0"
                      value={formData.late_payments_12m}
                      onChange={handleChange}
                    />
                  </div>
                </div>
              </div>

              <div className="rounded-lg border border-blue-200/50 bg-blue-50/50 dark:border-blue-900/50 dark:bg-blue-900/10 p-4">
                <h3 className="mb-4 text-lg font-semibold text-foreground">Data Privacy & Consent</h3>
                <div className="space-y-3">
                  <div className="flex items-start gap-3">
                    <Checkbox
                      id="dataProcessing"
                      checked={consent.dataProcessing}
                      onCheckedChange={() => handleConsentChange('dataProcessing')}
                    />
                    <label htmlFor="dataProcessing" className="text-sm text-foreground cursor-pointer">
                      I consent to the processing of my personal data for loan evaluation purposes in accordance with the data protection policy.
                    </label>
                  </div>
                  <div className="flex items-start gap-3">
                    <Checkbox
                      id="creditCheck"
                      checked={consent.creditCheck}
                      onCheckedChange={() => handleConsentChange('creditCheck')}
                    />
                    <label htmlFor="creditCheck" className="text-sm text-foreground cursor-pointer">
                      I authorize a credit check and inquiries from credit bureaus and other financial institutions.
                    </label>
                  </div>
                  <div className="flex items-start gap-3">
                    <Checkbox
                      id="dataSharing"
                      checked={consent.dataSharing}
                      onCheckedChange={() => handleConsentChange('dataSharing')}
                    />
                    <label htmlFor="dataSharing" className="text-sm text-foreground cursor-pointer">
                      I consent to data sharing with trusted partners for fraud prevention and risk assessment.
                    </label>
                  </div>
                  <div className="flex items-start gap-3">
                    <Checkbox
                      id="mpesaVerification"
                      checked={consent.mpesaVerification}
                      onCheckedChange={() => handleConsentChange('mpesaVerification')}
                    />
                    <label htmlFor="mpesaVerification" className="text-sm text-foreground cursor-pointer">
                      I accept the use of my M-Pesa statement for verification and income assessment purposes. I understand this data will be securely processed and retained only for the duration of the loan evaluation.
                    </label>
                  </div>
                </div>
              </div>

              <Button type="submit" className="w-full flex gap-2" disabled={isLoading || !consent.dataProcessing || !consent.creditCheck || !consent.mpesaVerification}>
                <Zap className="h-4 w-4" />
                {isLoading ? 'Generating Prediction...' : 'Generate Prediction'}
              </Button>
            </form>
          </CardContent>
        </Card>

        {/* Results */}
        <div className="space-y-6">
          {prediction ? (
            <>
              <Card>
                <CardHeader className="flex flex-row items-center justify-between">
                  <CardTitle>Prediction Result</CardTitle>
                  <Button variant="outline" size="sm" className="flex gap-2" onClick={downloadReport}>
                    <Download className="h-4 w-4" />
                    Download Report
                  </Button>
                </CardHeader>
                <CardContent className="space-y-4">
                  <div className="rounded-lg border-2 border-green-500/30 bg-green-500/10 p-4">
                    <div className="flex items-center gap-2">
                      <AlertCircle className="h-5 w-5 text-green-600" />
                      <span className="font-medium text-green-600">APPROVE</span>
                    </div>
                    <p className="mt-2 text-sm text-green-700">
                      This loan application has a low risk of default and is recommended for approval.
                    </p>
                  </div>

                  <div className="space-y-3">
                    <div>
                      <p className="text-sm text-muted-foreground">Default Probability</p>
                      <p className="text-2xl font-bold text-foreground">12.5%</p>
                    </div>
                    <div>
                      <p className="text-sm text-muted-foreground">Approval Score</p>
                      <p className="text-2xl font-bold text-primary">8.2/10</p>
                    </div>
                  </div>
                </CardContent>
              </Card>

              <Card>
                <CardHeader>
                  <CardTitle className="text-lg">Feature Importance</CardTitle>
                </CardHeader>
                <CardContent className="space-y-3">
                  {[
                    { name: 'Credit Score', importance: 0.35 },
                    { name: 'Income', importance: 0.25 },
                    { name: 'DTI Ratio', importance: 0.2 },
                    { name: 'Loan Amount', importance: 0.12 },
                    { name: 'Employment', importance: 0.08 },
                  ].map((item, i) => (
                    <div key={i}>
                      <div className="flex justify-between mb-1">
                        <span className="text-sm font-medium text-foreground">{item.name}</span>
                        <span className="text-sm text-muted-foreground">{(item.importance * 100).toFixed(0)}%</span>
                      </div>
                      <div className="h-2 rounded-full bg-muted">
                        <div
                          className="h-full rounded-full bg-primary"
                          style={{ width: `${item.importance * 100}%` }}
                        />
                      </div>
                    </div>
                  ))}
                </CardContent>
              </Card>
            </>
          ) : (
            <Card className="flex items-center justify-center h-64">
              <CardContent className="text-center">
                <p className="text-muted-foreground">Fill in the form and submit to see predictions</p>
              </CardContent>
            </Card>
          )}
        </div>
      </div>
    </div>
  )
}
